# Análisis clínico genómico de Fenilcetonuria

Proyecto compatible con **Jupyter Book 2 / MyST**.

## Construir HTML

```bash
uv sync
uv run jupyter book build --html .
uv run jupyter book start
```

```bash
explorer.exe _build/html/index.html
```
